name: 2023-cron
on:
  schedule:
    - cron: "30 23 * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call our API route to recalculate user rate statistics
        run: |
          curl --request POST \
          --url 'https://rogerbot.tech/api/2023-cron' \
          --header 'Authorization: Bearer ${{ secrets.CRON_API_KEY }}'
